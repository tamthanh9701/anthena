{
  "name": "anthena-figma-plugin",
  "version": "2.0.0",
  "description": "Anthena Sync — Figma plugin for applying curated releases to an owned clone of the AntD kit",
  "private": true,
  "scripts": {
    "build": "node build-figma.js",
    "package": "node build-figma.js --package",
    "test": "npx vitest run --config vitest.config.js"
  },
  "devDependencies": {
    "adm-zip": "^0.5.18",
    "esbuild": "^0.28.1",
    "vitest": "^4.1.9"
  }
}